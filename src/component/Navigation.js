import React, { Component } from "react";

const Navigation = () => {
  return (
    <nav style={{ display: "flex", justifyContent: "flex-end" }}>
      <p className="dim fa3 pa3 underline pointer">Sign Out</p>
    </nav>
  );
};

export default Navigation;
